POSTGRES_USER=devuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=market_pulse_dev

DATABASE_URL=postgres://devuser:changeme@localhost:5432/market_pulse_dev

DOTENV_CONFIG_QUIET=true

JWT_SECRET=replace-with-a-long-random-secret-generated-per-environment

FINNHUB_API_KEY=your_finnhub_api_key_here

FRONTEND_ORIGIN=http://localhost:3000

GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Must exactly match an Authorized redirect URI registered on this
# client in Google Cloud Console, e.g. http://localhost:3001/auth/google/callback
# for local dev, or the deployed backend's own /auth/google/callback in prod.
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback
